# 保电方案智能体 - 快速实现指南

> **状态**: 后端完成 ✅ | 前端API完成 ✅ | 前端UI待实现 ⏳  
> **预计完成时间**: 10分钟

---

## ✅ 已完成部分

### 1. 后端实现 (100%)

- **文件**: `aperag/agent/specialists/power_guarantee_agent.py`
- **类名**: `PowerGuaranteeAgent`
- **功能**:
  - 保电方案生成
  - 巡检计划制定
  - 应急资源准备
  - 风险评估

### 2. 前端API定义 (100%)

- **文件**: `web/src/lib/api/agents.ts`
- **类型**: `PowerGuaranteeRequest`, `PowerGuaranteeResponse`
- **方法**:
  - `generatePowerGuaranteePlan()` - 生成保电方案
  - `generateInspectionPlan()` - 生成巡检计划
  - `prepareResources()` - 准备应急资源

---

## ⏳ 待完成部分

### 1. 创建专属工作台页面

**步骤1**: 创建目录
```bash
mkdir web/src/app/workspace/agents/specific/power_guarantee
```

**步骤2**: 创建页面文件  
**文件**: `web/src/app/workspace/agents/specific/power_guarantee/page.tsx`

**参考**: 复制 `accident_deduction/page.tsx` 并修改：

```typescript
"use client";

import { useState } from 'react';
import { PageContainer, PageHeader, PageContent } from "@/components/page-container";
import { Card, CardHeader, CardTitle, CardContent, CardDescription } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Shield, Search, Layers, Zap, MessageSquare, Loader2, Brain, ChevronRight, ChevronDown } from "lucide-react";
import { Separator } from "@/components/ui/separator";
import { Textarea } from "@/components/ui/textarea";
import { Badge } from "@/components/ui/badge";
import { useTranslations } from "next-intl";
import { useAppContext } from '@/components/providers/app-provider';
import { agentAPI, PowerGuaranteeResponse, ThinkingStep } from '@/lib/api/agents';
import { toast } from 'sonner';

export default function PowerGuaranteeWorkspacePage() {
  const t = useTranslations("sidebar_workspace"); 
  const { user } = useAppContext();
  const [query, setQuery] = useState('');
  const [loading, setLoading] = useState(false);
  const [result, setResult] = useState<PowerGuaranteeResponse | null>(null);
  const [showThinking, setShowThinking] = useState(true);

  const handleStartTask = async () => {
    if (!query.trim()) {
      toast.error('请输入任务指令');
      return;
    }

    setLoading(true);
    setResult(null);
    try {
      const response = await agentAPI.generatePowerGuaranteePlan({
        query,
        user_id: user?.id || 'user-1',
      });
      setResult(response);
      if (response.success) {
        toast.success('保电方案生成成功');
      }
    } catch (error) {
      console.error('Task failed:', error);
      toast.error('任务执行失败，请稍后重试');
    } finally {
      setLoading(false);
    }
  };

  const handleQuickTask = async (taskType: 'plan' | 'inspection' | 'resources', taskQuery: string) => {
    setQuery(taskQuery);
    setLoading(true);
    setResult(null);
    try {
      let response;
      if (taskType === 'plan') {
        response = await agentAPI.generatePowerGuaranteePlan({
          query: taskQuery,
          user_id: user?.id || 'user-1',
        });
      } else if (taskType === 'inspection') {
        response = await agentAPI.generateInspectionPlan({
          query: taskQuery,
          user_id: user?.id || 'user-1',
        });
      } else {
        response = await agentAPI.prepareResources({
          query: taskQuery,
          user_id: user?.id || 'user-1',
        });
      }
      setResult(response);
      if (response.success) {
        toast.success('任务执行成功');
      }
    } catch (error) {
      console.error('Quick task failed:', error);
      toast.error('任务执行失败，请稍后重试');
    } finally {
      setLoading(false);
    }
  };

  return (
    <PageContainer>
      <PageHeader 
        breadcrumbs={[
          { title: t('agents'), href: '/workspace/agents' },
          { title: "保电方案专家 工作台" }
        ]}
      />
      <PageContent>
        <div className="space-y-6">
          {/* Header Section */}
          <div className="relative overflow-hidden rounded-xl border border-emerald-600/20 bg-gradient-to-br from-emerald-600/10 via-emerald-600/5 to-background p-8">
            <div className="relative z-10">
              <div className="flex items-center space-x-4 mb-6">
                <div className="p-3 rounded-full bg-emerald-600/20 backdrop-blur-sm">
                  <Shield className="w-8 h-8 text-emerald-600" />
                </div>
                <div>
                  <h1 className="text-3xl font-bold tracking-tight">保电方案专家 工作台</h1>
                  <p className="text-muted-foreground mt-1">
                    制定重要活动保电方案，确保关键时期供电万无一失。
                  </p>
                </div>
              </div>
            </div>
          </div>

          {/* Results Section */}
          {result && (
            <div className="space-y-6 animate-in fade-in slide-in-from-bottom-4 duration-500">
              {/* Thinking Stream */}
              {result.thinking_stream && result.thinking_stream.length > 0 && (
                <Card className="border-blue-100 bg-blue-50/50 dark:border-blue-900/50 dark:bg-blue-900/10">
                  <CardHeader className="pb-2">
                    <div
                      className="flex items-center justify-between cursor-pointer"
                      onClick={() => setShowThinking(!showThinking)}
                    >
                      <CardTitle className="flex items-center text-base text-blue-700 dark:text-blue-400">
                        <Brain className="mr-2 h-5 w-5" />
                        思考过程
                      </CardTitle>
                      {showThinking ? <ChevronDown className="h-4 w-4" /> : <ChevronRight className="h-4 w-4" />}
                    </div>
                  </CardHeader>
                  {showThinking && (
                    <CardContent className="space-y-3 pt-0">
                      {result.thinking_stream.map((step, index) => (
                        <div key={index} className="flex items-start gap-3 text-sm">
                          <Badge variant="secondary" className="mt-0.5 shrink-0 bg-blue-100 text-blue-700 hover:bg-blue-200 dark:bg-blue-900/40 dark:text-blue-300">
                            {step.step_type}
                          </Badge>
                          <span className="text-muted-foreground">{step.description}</span>
                        </div>
                      ))}
                    </CardContent>
                  )}
                </Card>
              )}

              {/* Answer */}
              {result.answer && (
                <Card className="border-emerald-200 bg-emerald-50/30 dark:border-emerald-900/30 dark:bg-emerald-900/5">
                  <CardHeader>
                    <CardTitle className="flex items-center text-lg">
                      <Shield className="mr-2 h-5 w-5 text-emerald-600" />
                      保电方案
                    </CardTitle>
                  </CardHeader>
                  <CardContent>
                    <div
                      className="prose prose-emerald dark:prose-invert max-w-none whitespace-pre-wrap"
                      dangerouslySetInnerHTML={{ __html: result.answer }}
                    />
                  </CardContent>
                </Card>
              )}

              {/* Plan Details */}
              {result.plan && result.plan.measures && (
                <div className="grid gap-4 md:grid-cols-2">
                  {result.plan.measures.slice(0, 4).map((measure, index) => (
                    <Card key={index}>
                      <CardHeader>
                        <CardTitle className="text-base">{measure.category}</CardTitle>
                      </CardHeader>
                      <CardContent className="space-y-2">
                        {measure.items.map((item, idx) => (
                          <div key={idx} className="flex items-start gap-2">
                            <span className="text-emerald-600 mt-1">•</span>
                            <span className="text-sm">{item}</span>
                          </div>
                        ))}
                      </CardContent>
                    </Card>
                  ))}
                </div>
              )}
            </div>
          )}

          {/* Initial Dashboard */}
          {!result && !loading && (
            <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
              <Card className="lg:col-span-2">
                <CardHeader>
                  <CardTitle className="flex items-center space-x-2 text-lg">
                    <Zap className="w-5 h-5 text-emerald-600" />
                    <span>核心能力</span>
                  </CardTitle>
                  <CardDescription>选择一项能力开始任务，或直接在下方输入指令。</CardDescription>
                </CardHeader>
                <CardContent className="space-y-6">
                  <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
                    <div 
                      className="p-4 border rounded-lg hover:bg-muted/50 cursor-pointer transition-colors hover:border-emerald-600/50"
                      onClick={() => handleQuickTask('plan', '编制高考保电方案')}
                    >
                      <h4 className="font-medium text-sm mb-1">保电方案</h4>
                      <p className="text-xs text-muted-foreground">编制重要活动保电工作方案</p>
                    </div>
                    <div 
                      className="p-4 border rounded-lg hover:bg-muted/50 cursor-pointer transition-colors hover:border-emerald-600/50"
                      onClick={() => handleQuickTask('inspection', '制定保电巡检计划')}
                    >
                      <h4 className="font-medium text-sm mb-1">巡检计划</h4>
                      <p className="text-xs text-muted-foreground">制定设备巡检计划</p>
                    </div>
                    <div 
                      className="p-4 border rounded-lg hover:bg-muted/50 cursor-pointer transition-colors hover:border-emerald-600/50"
                      onClick={() => handleQuickTask('resources', '准备应急资源')}
                    >
                      <h4 className="font-medium text-sm mb-1">资源准备</h4>
                      <p className="text-xs text-muted-foreground">准备应急资源清单</p>
                    </div>
                  </div>
                  <Separator />
                  <div className="space-y-2">
                    <h3 className="text-sm font-medium">任务指令</h3>
                    <Textarea 
                      placeholder="例如：编制春节保电工作方案"
                      rows={4}
                      value={query}
                      onChange={(e) => setQuery(e.target.value)}
                      onKeyDown={(e) => {
                        if (e.key === 'Enter' && e.ctrlKey) {
                          handleStartTask();
                        }
                      }}
                    />
                    <p className="text-xs text-muted-foreground">提示: 按 Ctrl+Enter 快速发送</p>
                  </div>
                  <Button 
                    onClick={handleStartTask} 
                    disabled={loading || !query.trim()}
                    className="w-full bg-emerald-600 hover:bg-emerald-700"
                  >
                    {loading ? (
                      <>
                        <Loader2 className="w-4 h-4 mr-2 animate-spin" />
                        生成中...
                      </>
                    ) : (
                      <>
                        <MessageSquare className="w-4 h-4 mr-2" />
                        发送指令
                      </>
                    )}
                  </Button>
                </CardContent>
              </Card>

              <Card className="lg:col-span-1">
                <CardHeader>
                  <CardTitle className="text-lg flex items-center space-x-2">
                    <Layers className="w-5 h-5 text-primary" />
                    <span>快捷工具</span>
                  </CardTitle>
                </CardHeader>
                <CardContent className="space-y-4">
                  <div className="space-y-2">
                    <h3 className="text-sm font-medium">快速示例</h3>
                    <div className="space-y-2">
                      <Button 
                        variant="outline" 
                        className="w-full justify-start text-xs"
                        onClick={() => setQuery('编制高考保电方案')}
                      >
                        高考保电方案
                      </Button>
                      <Button 
                        variant="outline" 
                        className="w-full justify-start text-xs"
                        onClick={() => setQuery('编制春节保电工作方案')}
                      >
                        春节保电方案
                      </Button>
                      <Button 
                        variant="outline" 
                        className="w-full justify-start text-xs"
                        onClick={() => setQuery('制定重要会议保电方案')}
                      >
                        重要会议保电
                      </Button>
                    </div>
                  </div>
                  <Separator />
                  <div className="space-y-2">
                    <h3 className="text-sm font-medium">历史记录</h3>
                    <div className="text-sm text-muted-foreground text-center py-4">
                      暂无最近任务记录
                    </div>
                  </div>
                </CardContent>
              </Card>
            </div>
          )}
        </div>
      </PageContent>
    </PageContainer>
  );
}
```

### 2. 更新智能体列表页

**文件**: `web/src/app/workspace/agents/page.tsx`

在 `SPECIALIST_AGENTS` 数组中添加（已存在，只需确认）：

```typescript
{
  id: 'power_guarantee',
  title: 'The Power Guarantee Agent (保电方案专家)',
  description:
    '保电方案编制专家。制定重要活动保电方案，设备巡检计划，应急资源配置。',
  icon: Shield,
  color: 'text-emerald-500',
  bg: 'bg-emerald-500/10',
  capabilities: ['保电方案', '巡检计划', '资源配置'],
},
```

在 `SPECIAL_AGENT_ROUTES` 中添加：

```typescript
power_guarantee: '/workspace/agents/specific/power_guarantee',
```

---

## 🚀 快速完成步骤

### 选项A: 手动创建（10分钟）

1. 创建目录和页面文件（参考上面的代码）
2. 重新构建前端：`docker-compose up -d --build frontend`
3. 访问测试：`http://localhost:3000/workspace/agents/specific/power_guarantee`

### 选项B: 复制现有页面（5分钟）

```bash
# 复制事故预想页面作为模板
cp -r web/src/app/workspace/agents/specific/accident_deduction web/src/app/workspace/agents/specific/power_guarantee

# 然后修改 page.tsx 中的：
# - 颜色：pink → emerald
# - 图标：Activity → Shield
# - 标题：事故预想 → 保电方案
# - API调用：agentAPI.generateAccidentDeduction → agentAPI.generatePowerGuaranteePlan
```

---

## 📝 测试用例

### 测试用例1: 保电方案生成
**输入**: `编制高考保电方案`

**预期输出**:
- 保电组织机构
- 风险评估
- 保障措施（组织、设备、运行、应急、禁止性）
- 巡检计划
- 应急处置预案
- 资源准备
- 信息报送机制

### 测试用例2: 巡检计划
**输入**: `制定保电巡检计划`

**预期输出**:
- 设备巡检清单
- 巡检频次
- 巡检要点

### 测试用例3: 资源准备
**输入**: `准备应急资源`

**预期输出**:
- 人员配置
- 装备配置
- 物资储备

---

## ✅ 验证清单

- [ ] 后端智能体已实现
- [ ] 前端API已定义
- [ ] 专属工作台页面已创建
- [ ] 智能体列表页已更新
- [ ] 路由配置已添加
- [ ] 前端重新构建
- [ ] 功能测试通过

---

## 🎯 总结

保电方案智能体的实现与事故预想智能体完全相同的模式：

1. **后端**: ✅ 已完成（PowerGuaranteeAgent）
2. **API**: ✅ 已完成（agents.ts）
3. **前端UI**: ⏳ 只需复制模板并修改

**预计完成时间**: 5-10分钟

**参考文档**: 
- 事故预想智能体实现（完全相同的模式）
- `accident_deduction/page.tsx`（最佳模板）
