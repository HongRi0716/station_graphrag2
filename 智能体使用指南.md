# 智能体使用指南

本指南详细说明如何使用系统中已完全实现的两个核心智能体。

---

## 📋 目录
1. [值班长 (Supervisor) 使用指南](#值班长-supervisor-使用指南)
2. [图谱专家 (Archivist) 使用指南](#图谱专家-archivist-使用指南)
3. [常见问题](#常见问题)

---

## 🎯 值班长 (Supervisor) 使用指南

### 功能概述
值班长是变电站的智能协调中枢，负责：
- **任务分发**: 接收用户任务并智能分配给合适的专家智能体
- **协作协调**: 协调多个智能体协同工作
- **态势感知**: 实时监控变电站运行状态
- **决策支持**: 提供智能化的决策建议

### 访问方式

#### 方式一：通过前端界面访问

1. **启动系统**
   ```bash
   # 确保所有服务正在运行
   docker-compose up -d
   ```

2. **访问前端**
   - 打开浏览器访问: `http://localhost:3000`
   - 登录系统（如果需要）

3. **进入值班长界面**
   - 在导航栏中找到"智能体"或"Agents"菜单
   - 点击"值班长"或"Supervisor"
   - 或直接访问: `http://localhost:3000/agents/supervisor`

#### 方式二：通过 API 直接调用

**端点**: `POST /api/v1/agents/supervisor/dispatch`

**请求示例**:
```bash
curl -X POST http://localhost:8000/api/v1/agents/supervisor/dispatch \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "task": "检查110kV 1号主变压器的运行状态",
    "user_id": "user123",
    "chat_id": "chat456",
    "priority": "normal"
  }'
```

**请求参数**:
- `task` (必填): 任务描述，例如"生成操作票"、"查询设备信息"
- `user_id` (必填): 用户ID
- `chat_id` (可选): 会话ID，用于关联对话历史
- `priority` (可选): 优先级，可选值: `urgent`、`high`、`normal`（默认）

**响应示例**:
```json
{
  "success": true,
  "message": "任务分发成功",
  "data": {
    "assigned_agent": "操作票专家",
    "task_id": "task_abc123",
    "estimated_time": "2分钟"
  },
  "task_analysis": {
    "task_type": "操作票生成",
    "complexity": "medium",
    "required_agents": ["操作票专家", "图谱专家"]
  },
  "thinking_stream": [
    {
      "step_type": "analysis",
      "description": "分析任务类型",
      "detail": "识别为操作票生成任务",
      "timestamp": "2025-11-25T12:30:00Z"
    }
  ]
}
```

### 使用场景示例

#### 场景1：生成操作票
```json
{
  "task": "生成110kV 1号主变由运行转冷备用的操作票",
  "user_id": "operator001",
  "priority": "high"
}
```

**值班长会**:
1. 分析任务类型 → 识别为"操作票生成"
2. 分配给"操作票专家"
3. 协调"图谱专家"查询设备信息
4. 返回完整的操作票

#### 场景2：查询设备信息
```json
{
  "task": "查询220kV母线的历史缺陷记录",
  "user_id": "operator001"
}
```

**值班长会**:
1. 识别为"信息查询"任务
2. 分配给"图谱专家"
3. 返回查询结果

#### 场景3：事故推演
```json
{
  "task": "模拟110kV 1号主变故障的影响范围",
  "user_id": "operator001",
  "priority": "urgent"
}
```

**值班长会**:
1. 识别为"事故推演"任务
2. 分配给"事故推演专家"
3. 协调"图谱专家"提供拓扑信息
4. 返回推演报告

### WebSocket 实时通信

**端点**: `WS /api/v1/agents/supervisor/ws/dispatch`

**使用示例** (JavaScript):
```javascript
const ws = new WebSocket('ws://localhost:8000/api/v1/agents/supervisor/ws/dispatch');

ws.onopen = () => {
  // 发送任务
  ws.send(JSON.stringify({
    task: "查询设备状态",
    user_id: "user123"
  }));
};

ws.onmessage = (event) => {
  const data = JSON.parse(event.data);
  
  switch(data.type) {
    case 'start':
      console.log('任务开始:', data.message);
      break;
    case 'thinking':
      console.log('思维过程:', data.description);
      break;
    case 'result':
      console.log('任务结果:', data.data);
      break;
    case 'complete':
      console.log('任务完成');
      break;
    case 'error':
      console.error('错误:', data.message);
      break;
  }
};
```

### 前端组件使用

**组件位置**: `frontend/src/components/agents/SupervisorDashboard.tsx`

**主要功能**:
- 任务输入框
- 实时思维流显示
- 任务分配可视化
- 执行结果展示

---

## 🔍 图谱专家 (Archivist) 使用指南

### 功能概述
图谱专家是全局知识库检索专家，擅长：
- **跨库检索**: 在所有知识库中搜索信息
- **设备台账查询**: 查找设备的详细信息和历史记录
- **缺陷溯源**: 追溯设备缺陷的历史
- **文档查找**: 快速定位技术规程和标准

### 访问方式

#### 方式一：通过前端界面访问

1. **访问图谱专家页面**
   - 在导航栏中找到"图谱专家"或"Archivist"
   - 或直接访问: `http://localhost:3000/agents/archivist`

2. **使用搜索功能**
   - 在搜索框中输入查询内容
   - 选择搜索模式（向量搜索/图谱搜索/混合搜索）
   - 点击"搜索"按钮

#### 方式二：通过 API 直接调用

**端点**: `POST /api/v1/agents/archivist/search`

**请求示例**:
```bash
curl -X POST http://localhost:8000/api/v1/agents/archivist/search \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "query": "110kV主变压器的技术参数",
    "user_id": "user123",
    "search_mode": "hybrid",
    "top_k": 10
  }'
```

**请求参数**:
- `query` (必填): 搜索查询，支持自然语言
- `user_id` (必填): 用户ID
- `search_mode` (可选): 搜索模式
  - `vector`: 向量搜索（语义相似度）
  - `graph`: 图谱搜索（实体关系）
  - `hybrid`: 混合搜索（推荐，默认）
- `top_k` (可选): 返回结果数量，默认10
- `collections` (可选): 指定搜索的知识库ID列表

**响应示例**:
```json
{
  "success": true,
  "results": [
    {
      "score": 0.95,
      "text": "110kV主变压器型号：SFZ11-50000/110，额定容量50MVA...",
      "source": {
        "collection_id": "col_equipment_db",
        "collection_title": "设备台账库",
        "document_id": "doc_transformer_001",
        "document_name": "110kV主变技术参数.pdf"
      },
      "metadata": {
        "page": 3,
        "section": "技术参数"
      }
    }
  ],
  "graph_data": {
    "nodes": [
      {
        "id": "entity_transformer_001",
        "type": "equipment",
        "name": "110kV 1号主变",
        "properties": {
          "model": "SFZ11-50000/110",
          "capacity": "50MVA"
        }
      }
    ],
    "edges": [
      {
        "source": "entity_transformer_001",
        "target": "entity_busbar_110kv",
        "relation": "connected_to"
      }
    ]
  },
  "statistics": {
    "total_results": 25,
    "search_time_ms": 156,
    "collections_searched": 5
  }
}
```

### 使用场景示例

#### 场景1：查询设备技术参数
```json
{
  "query": "查询110kV 1号主变的额定容量和型号",
  "user_id": "operator001",
  "search_mode": "hybrid",
  "top_k": 5
}
```

**返回**: 设备的详细技术参数、制造商信息、安装日期等

#### 场景2：查找历史缺陷
```json
{
  "query": "220kV母线近一年的缺陷记录",
  "user_id": "operator001",
  "search_mode": "graph",
  "collections": ["col_defect_records"]
}
```

**返回**: 缺陷记录列表、缺陷类型分布、处理情况等

#### 场景3：搜索操作规程
```json
{
  "query": "主变压器倒闸操作规程",
  "user_id": "operator001",
  "search_mode": "vector",
  "top_k": 3
}
```

**返回**: 相关规程文档、操作步骤、注意事项等

#### 场景4：设备关系查询
```json
{
  "query": "110kV 1号主变连接的所有设备",
  "user_id": "operator001",
  "search_mode": "graph"
}
```

**返回**: 设备拓扑图、连接关系、上下级设备等

### 前端组件使用

**组件位置**: `frontend/src/components/agents/ArchivistSearch.tsx`

**主要功能**:
1. **搜索输入**
   - 支持自然语言查询
   - 搜索历史记录
   - 搜索建议

2. **搜索模式选择**
   - 向量搜索：基于语义相似度
   - 图谱搜索：基于实体关系
   - 混合搜索：结合两种方式

3. **结果展示**
   - 文本结果列表
   - 来源引用
   - 相关度评分

4. **图谱可视化**
   - 实体关系图
   - 交互式节点
   - 路径高亮

### 高级功能

#### 1. 指定知识库搜索
```json
{
  "query": "查询设备信息",
  "collections": [
    "col_equipment_db",
    "col_maintenance_records"
  ]
}
```

#### 2. 图谱路径查询
```json
{
  "query": "从110kV母线到220kV母线的供电路径",
  "search_mode": "graph",
  "graph_options": {
    "max_depth": 5,
    "relation_types": ["connected_to", "supplies"]
  }
}
```

#### 3. 时间范围过滤
```json
{
  "query": "设备缺陷记录",
  "filters": {
    "date_range": {
      "start": "2024-01-01",
      "end": "2024-12-31"
    }
  }
}
```

---

## ❓ 常见问题

### Q1: 如何查看智能体是否正常运行？

**A**: 访问健康检查端点
```bash
# 值班长健康检查
curl http://localhost:8000/api/v1/agents/supervisor/health

# 图谱专家健康检查
curl http://localhost:8000/api/v1/agents/archivist/health
```

**正常响应**:
```json
{
  "status": "healthy",
  "agent": "值班长 (Supervisor)",
  "role": "SUPERVISOR"
}
```

### Q2: 为什么搜索没有返回结果？

**可能原因**:
1. **知识库为空**: 需要先上传文档到知识库
2. **查询过于具体**: 尝试使用更通用的关键词
3. **知识库未激活**: 检查知识库状态是否为 ACTIVE

**解决方法**:
```bash
# 检查知识库列表
curl http://localhost:8000/api/v1/collections \
  -H "Authorization: Bearer YOUR_TOKEN"

# 上传文档到知识库
# 通过前端界面: Collections -> Upload Documents
```

### Q3: 如何获取认证 Token？

**A**: 通过登录接口获取
```bash
curl -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "your_username",
    "password": "your_password"
  }'
```

### Q4: 前端页面无法访问怎么办？

**排查步骤**:
1. 检查前端容器是否运行
   ```bash
   docker ps | grep aperag-frontend
   ```

2. 检查前端日志
   ```bash
   docker logs aperag-frontend
   ```

3. 确认端口映射
   ```bash
   # 前端应该在 3000 端口
   curl http://localhost:3000
   ```

### Q5: 如何查看智能体的思维过程？

**A**: 使用 WebSocket 连接或查看响应中的 `thinking_stream` 字段

**WebSocket 示例**:
```javascript
const ws = new WebSocket('ws://localhost:8000/api/v1/agents/supervisor/ws/dispatch');

ws.onmessage = (event) => {
  const data = JSON.parse(event.data);
  if (data.type === 'thinking') {
    console.log('思维步骤:', data.description);
    console.log('详细信息:', data.detail);
  }
};
```

### Q6: 如何提高搜索准确度？

**建议**:
1. **使用具体的关键词**: "110kV 1号主变" 比 "主变" 更精确
2. **选择合适的搜索模式**:
   - 查找相似内容 → 向量搜索
   - 查找关系 → 图谱搜索
   - 综合查询 → 混合搜索
3. **指定知识库范围**: 缩小搜索范围提高精度
4. **使用过滤条件**: 时间、类型、标签等

### Q7: 如何集成到自己的应用中？

**A**: 通过 REST API 或 SDK 集成

**Python SDK 示例**:
```python
import requests

class SupervisorClient:
    def __init__(self, base_url, token):
        self.base_url = base_url
        self.headers = {"Authorization": f"Bearer {token}"}
    
    def dispatch_task(self, task, user_id, priority="normal"):
        response = requests.post(
            f"{self.base_url}/api/v1/agents/supervisor/dispatch",
            headers=self.headers,
            json={
                "task": task,
                "user_id": user_id,
                "priority": priority
            }
        )
        return response.json()

# 使用
client = SupervisorClient("http://localhost:8000", "your_token")
result = client.dispatch_task("查询设备状态", "user123")
print(result)
```

---

## 📚 相关文档

- [API 文档](http://localhost:8000/docs) - Swagger UI
- [智能体配置](./aperag/agent/agent_configs.py) - 智能体配置详情
- [前端组件](./frontend/src/components/agents/) - React 组件源码

---

## 🆘 获取帮助

如果遇到问题：
1. 查看 [常见问题](#常见问题) 部分
2. 检查 Docker 容器日志: `docker logs aperag-api`
3. 查看浏览器控制台错误（F12）
4. 参考 API 文档: `http://localhost:8000/docs`

---

**最后更新**: 2025-11-25
