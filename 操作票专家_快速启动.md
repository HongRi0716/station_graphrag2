# 操作票专家 - 快速启动指南

## 🚀 立即开始使用

### 方式一：通过前端界面

1. **访问工作台**
   ```
   http://localhost:3000/workspace/agents/specific/operation_ticket
   ```

2. **选择快捷任务**
   - 点击"主变转检修"卡片
   - 或点击"母线倒闸"卡片
   - 或点击"设备投运"卡片

3. **查看结果**
   - 思维链实时显示
   - 操作票自动生成
   - 安全检查结果展示

### 方式二：通过API调用

```bash
curl -X POST http://localhost:8000/api/v1/agents/operation-ticket/generate \
  -H "Content-Type: application/json" \
  -d '{
    "task": "生成#1主变转冷备用操作票",
    "user_id": "test_user"
  }'
```

### 方式三：通过Python代码

```python
import requests

response = requests.post(
    'http://localhost:8000/api/v1/agents/operation-ticket/generate',
    json={
        'task': '生成#1主变转冷备用操作票',
        'user_id': 'test_user'
    }
)

result = response.json()
print(result['report'])  # 打印操作票
```

## 📋 示例任务

### 1. 主变转检修
```
生成#1主变转冷备用操作票
```

### 2. 母线倒闸
```
生成110kV母线I母转II母操作票
```

### 3. 设备投运
```
生成#2主变投运送电操作票
```

## 🔧 重启服务（如需要）

```bash
# 重启后端
docker-compose restart api

# 重启前端（如果修改了前端代码）
docker-compose restart frontend
```

## ✅ 验证服务状态

```bash
# 检查API健康状态
curl http://localhost:8000/api/v1/agents/operation-ticket/health

# 预期响应
{
  "status": "healthy",
  "agent": "操作票专家 (Operation Ticket Agent)",
  "role": "operation_ticket"
}
```

## 🎯 快速测试

1. 打开浏览器访问: `http://localhost:3000/workspace/agents/specific/operation_ticket`
2. 点击"主变转冷备用"快捷按钮
3. 等待10-30秒
4. 查看生成的操作票

## 📞 问题排查

### 前端无法访问？
```bash
docker-compose ps frontend
docker-compose logs frontend
```

### API报错？
```bash
docker-compose ps api
docker-compose logs api
```

### 智能体未注册？
```bash
docker-compose restart api
# 等待30秒后重试
```

---

**准备好了吗？** 现在就去试试吧！ 🚀
